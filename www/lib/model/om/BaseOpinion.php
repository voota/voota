<?php

/**
 * Base class that represents a row from the 'opinion' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Sat Aug 15 18:10:54 2009
 *
 * @package    lib.model.om
 */
abstract class BaseOpinion extends BaseObject  implements Persistent {


  const PEER = 'OpinionPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        OpinionPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the valor field.
	 * @var        int
	 */
	protected $valor;

	/**
	 * The value for the texto field.
	 * @var        string
	 */
	protected $texto;

	/**
	 * The value for the usuario_id field.
	 * @var        int
	 */
	protected $usuario_id;

	/**
	 * The value for the partido_id field.
	 * @var        int
	 */
	protected $partido_id;

	/**
	 * The value for the politico_id field.
	 * @var        int
	 */
	protected $politico_id;

	/**
	 * The value for the opinion_id field.
	 * @var        int
	 */
	protected $opinion_id;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * @var        Usuario
	 */
	protected $aUsuario;

	/**
	 * @var        Partido
	 */
	protected $aPartido;

	/**
	 * @var        Politico
	 */
	protected $aPolitico;

	/**
	 * @var        Opinion
	 */
	protected $aOpinionRelatedByOpinionId;

	/**
	 * @var        array Imagen[] Collection to store aggregation of Imagen objects.
	 */
	protected $collImagens;

	/**
	 * @var        Criteria The criteria used to select the current contents of collImagens.
	 */
	private $lastImagenCriteria = null;

	/**
	 * @var        array Opinion[] Collection to store aggregation of Opinion objects.
	 */
	protected $collOpinionsRelatedByOpinionId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collOpinionsRelatedByOpinionId.
	 */
	private $lastOpinionRelatedByOpinionIdCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseOpinion object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [valor] column value.
	 * 
	 * @return     int
	 */
	public function getValor()
	{
		return $this->valor;
	}

	/**
	 * Get the [texto] column value.
	 * 
	 * @return     string
	 */
	public function getTexto()
	{
		return $this->texto;
	}

	/**
	 * Get the [usuario_id] column value.
	 * 
	 * @return     int
	 */
	public function getUsuarioId()
	{
		return $this->usuario_id;
	}

	/**
	 * Get the [partido_id] column value.
	 * 
	 * @return     int
	 */
	public function getPartidoId()
	{
		return $this->partido_id;
	}

	/**
	 * Get the [politico_id] column value.
	 * 
	 * @return     int
	 */
	public function getPoliticoId()
	{
		return $this->politico_id;
	}

	/**
	 * Get the [opinion_id] column value.
	 * 
	 * @return     int
	 */
	public function getOpinionId()
	{
		return $this->opinion_id;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Opinion The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = OpinionPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [valor] column.
	 * 
	 * @param      int $v new value
	 * @return     Opinion The current object (for fluent API support)
	 */
	public function setValor($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->valor !== $v) {
			$this->valor = $v;
			$this->modifiedColumns[] = OpinionPeer::VALOR;
		}

		return $this;
	} // setValor()

	/**
	 * Set the value of [texto] column.
	 * 
	 * @param      string $v new value
	 * @return     Opinion The current object (for fluent API support)
	 */
	public function setTexto($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->texto !== $v) {
			$this->texto = $v;
			$this->modifiedColumns[] = OpinionPeer::TEXTO;
		}

		return $this;
	} // setTexto()

	/**
	 * Set the value of [usuario_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Opinion The current object (for fluent API support)
	 */
	public function setUsuarioId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->usuario_id !== $v) {
			$this->usuario_id = $v;
			$this->modifiedColumns[] = OpinionPeer::USUARIO_ID;
		}

		if ($this->aUsuario !== null && $this->aUsuario->getId() !== $v) {
			$this->aUsuario = null;
		}

		return $this;
	} // setUsuarioId()

	/**
	 * Set the value of [partido_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Opinion The current object (for fluent API support)
	 */
	public function setPartidoId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->partido_id !== $v) {
			$this->partido_id = $v;
			$this->modifiedColumns[] = OpinionPeer::PARTIDO_ID;
		}

		if ($this->aPartido !== null && $this->aPartido->getId() !== $v) {
			$this->aPartido = null;
		}

		return $this;
	} // setPartidoId()

	/**
	 * Set the value of [politico_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Opinion The current object (for fluent API support)
	 */
	public function setPoliticoId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->politico_id !== $v) {
			$this->politico_id = $v;
			$this->modifiedColumns[] = OpinionPeer::POLITICO_ID;
		}

		if ($this->aPolitico !== null && $this->aPolitico->getId() !== $v) {
			$this->aPolitico = null;
		}

		return $this;
	} // setPoliticoId()

	/**
	 * Set the value of [opinion_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Opinion The current object (for fluent API support)
	 */
	public function setOpinionId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->opinion_id !== $v) {
			$this->opinion_id = $v;
			$this->modifiedColumns[] = OpinionPeer::OPINION_ID;
		}

		if ($this->aOpinionRelatedByOpinionId !== null && $this->aOpinionRelatedByOpinionId->getId() !== $v) {
			$this->aOpinionRelatedByOpinionId = null;
		}

		return $this;
	} // setOpinionId()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Opinion The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = OpinionPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array())) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->valor = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->texto = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->usuario_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->partido_id = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->politico_id = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->opinion_id = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->created_at = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 8; // 8 = OpinionPeer::NUM_COLUMNS - OpinionPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Opinion object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aUsuario !== null && $this->usuario_id !== $this->aUsuario->getId()) {
			$this->aUsuario = null;
		}
		if ($this->aPartido !== null && $this->partido_id !== $this->aPartido->getId()) {
			$this->aPartido = null;
		}
		if ($this->aPolitico !== null && $this->politico_id !== $this->aPolitico->getId()) {
			$this->aPolitico = null;
		}
		if ($this->aOpinionRelatedByOpinionId !== null && $this->opinion_id !== $this->aOpinionRelatedByOpinionId->getId()) {
			$this->aOpinionRelatedByOpinionId = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(OpinionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = OpinionPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aUsuario = null;
			$this->aPartido = null;
			$this->aPolitico = null;
			$this->aOpinionRelatedByOpinionId = null;
			$this->collImagens = null;
			$this->lastImagenCriteria = null;

			$this->collOpinionsRelatedByOpinionId = null;
			$this->lastOpinionRelatedByOpinionIdCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseOpinion:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(OpinionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			OpinionPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseOpinion:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseOpinion:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(OpinionPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(OpinionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseOpinion:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			OpinionPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUsuario !== null) {
				if ($this->aUsuario->isModified() || $this->aUsuario->isNew()) {
					$affectedRows += $this->aUsuario->save($con);
				}
				$this->setUsuario($this->aUsuario);
			}

			if ($this->aPartido !== null) {
				if ($this->aPartido->isModified() || $this->aPartido->isNew()) {
					$affectedRows += $this->aPartido->save($con);
				}
				$this->setPartido($this->aPartido);
			}

			if ($this->aPolitico !== null) {
				if ($this->aPolitico->isModified() || $this->aPolitico->isNew()) {
					$affectedRows += $this->aPolitico->save($con);
				}
				$this->setPolitico($this->aPolitico);
			}

			if ($this->aOpinionRelatedByOpinionId !== null) {
				if ($this->aOpinionRelatedByOpinionId->isModified() || $this->aOpinionRelatedByOpinionId->isNew()) {
					$affectedRows += $this->aOpinionRelatedByOpinionId->save($con);
				}
				$this->setOpinionRelatedByOpinionId($this->aOpinionRelatedByOpinionId);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = OpinionPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = OpinionPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += OpinionPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collImagens !== null) {
				foreach ($this->collImagens as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collOpinionsRelatedByOpinionId !== null) {
				foreach ($this->collOpinionsRelatedByOpinionId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUsuario !== null) {
				if (!$this->aUsuario->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUsuario->getValidationFailures());
				}
			}

			if ($this->aPartido !== null) {
				if (!$this->aPartido->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPartido->getValidationFailures());
				}
			}

			if ($this->aPolitico !== null) {
				if (!$this->aPolitico->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPolitico->getValidationFailures());
				}
			}

			if ($this->aOpinionRelatedByOpinionId !== null) {
				if (!$this->aOpinionRelatedByOpinionId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aOpinionRelatedByOpinionId->getValidationFailures());
				}
			}


			if (($retval = OpinionPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collImagens !== null) {
					foreach ($this->collImagens as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collOpinionsRelatedByOpinionId !== null) {
					foreach ($this->collOpinionsRelatedByOpinionId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = OpinionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getValor();
				break;
			case 2:
				return $this->getTexto();
				break;
			case 3:
				return $this->getUsuarioId();
				break;
			case 4:
				return $this->getPartidoId();
				break;
			case 5:
				return $this->getPoliticoId();
				break;
			case 6:
				return $this->getOpinionId();
				break;
			case 7:
				return $this->getCreatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = OpinionPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getValor(),
			$keys[2] => $this->getTexto(),
			$keys[3] => $this->getUsuarioId(),
			$keys[4] => $this->getPartidoId(),
			$keys[5] => $this->getPoliticoId(),
			$keys[6] => $this->getOpinionId(),
			$keys[7] => $this->getCreatedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = OpinionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setValor($value);
				break;
			case 2:
				$this->setTexto($value);
				break;
			case 3:
				$this->setUsuarioId($value);
				break;
			case 4:
				$this->setPartidoId($value);
				break;
			case 5:
				$this->setPoliticoId($value);
				break;
			case 6:
				$this->setOpinionId($value);
				break;
			case 7:
				$this->setCreatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = OpinionPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setValor($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setTexto($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setUsuarioId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setPartidoId($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setPoliticoId($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setOpinionId($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setCreatedAt($arr[$keys[7]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(OpinionPeer::DATABASE_NAME);

		if ($this->isColumnModified(OpinionPeer::ID)) $criteria->add(OpinionPeer::ID, $this->id);
		if ($this->isColumnModified(OpinionPeer::VALOR)) $criteria->add(OpinionPeer::VALOR, $this->valor);
		if ($this->isColumnModified(OpinionPeer::TEXTO)) $criteria->add(OpinionPeer::TEXTO, $this->texto);
		if ($this->isColumnModified(OpinionPeer::USUARIO_ID)) $criteria->add(OpinionPeer::USUARIO_ID, $this->usuario_id);
		if ($this->isColumnModified(OpinionPeer::PARTIDO_ID)) $criteria->add(OpinionPeer::PARTIDO_ID, $this->partido_id);
		if ($this->isColumnModified(OpinionPeer::POLITICO_ID)) $criteria->add(OpinionPeer::POLITICO_ID, $this->politico_id);
		if ($this->isColumnModified(OpinionPeer::OPINION_ID)) $criteria->add(OpinionPeer::OPINION_ID, $this->opinion_id);
		if ($this->isColumnModified(OpinionPeer::CREATED_AT)) $criteria->add(OpinionPeer::CREATED_AT, $this->created_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(OpinionPeer::DATABASE_NAME);

		$criteria->add(OpinionPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Opinion (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setValor($this->valor);

		$copyObj->setTexto($this->texto);

		$copyObj->setUsuarioId($this->usuario_id);

		$copyObj->setPartidoId($this->partido_id);

		$copyObj->setPoliticoId($this->politico_id);

		$copyObj->setOpinionId($this->opinion_id);

		$copyObj->setCreatedAt($this->created_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getImagens() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addImagen($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getOpinionsRelatedByOpinionId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addOpinionRelatedByOpinionId($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Opinion Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     OpinionPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new OpinionPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Usuario object.
	 *
	 * @param      Usuario $v
	 * @return     Opinion The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUsuario(Usuario $v = null)
	{
		if ($v === null) {
			$this->setUsuarioId(NULL);
		} else {
			$this->setUsuarioId($v->getId());
		}

		$this->aUsuario = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Usuario object, it will not be re-added.
		if ($v !== null) {
			$v->addOpinion($this);
		}

		return $this;
	}


	/**
	 * Get the associated Usuario object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Usuario The associated Usuario object.
	 * @throws     PropelException
	 */
	public function getUsuario(PropelPDO $con = null)
	{
		if ($this->aUsuario === null && ($this->usuario_id !== null)) {
			$c = new Criteria(UsuarioPeer::DATABASE_NAME);
			$c->add(UsuarioPeer::ID, $this->usuario_id);
			$this->aUsuario = UsuarioPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUsuario->addOpinions($this);
			 */
		}
		return $this->aUsuario;
	}

	/**
	 * Declares an association between this object and a Partido object.
	 *
	 * @param      Partido $v
	 * @return     Opinion The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPartido(Partido $v = null)
	{
		if ($v === null) {
			$this->setPartidoId(NULL);
		} else {
			$this->setPartidoId($v->getId());
		}

		$this->aPartido = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Partido object, it will not be re-added.
		if ($v !== null) {
			$v->addOpinion($this);
		}

		return $this;
	}


	/**
	 * Get the associated Partido object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Partido The associated Partido object.
	 * @throws     PropelException
	 */
	public function getPartido(PropelPDO $con = null)
	{
		if ($this->aPartido === null && ($this->partido_id !== null)) {
			$c = new Criteria(PartidoPeer::DATABASE_NAME);
			$c->add(PartidoPeer::ID, $this->partido_id);
			$this->aPartido = PartidoPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aPartido->addOpinions($this);
			 */
		}
		return $this->aPartido;
	}

	/**
	 * Declares an association between this object and a Politico object.
	 *
	 * @param      Politico $v
	 * @return     Opinion The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPolitico(Politico $v = null)
	{
		if ($v === null) {
			$this->setPoliticoId(NULL);
		} else {
			$this->setPoliticoId($v->getId());
		}

		$this->aPolitico = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Politico object, it will not be re-added.
		if ($v !== null) {
			$v->addOpinion($this);
		}

		return $this;
	}


	/**
	 * Get the associated Politico object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Politico The associated Politico object.
	 * @throws     PropelException
	 */
	public function getPolitico(PropelPDO $con = null)
	{
		if ($this->aPolitico === null && ($this->politico_id !== null)) {
			$c = new Criteria(PoliticoPeer::DATABASE_NAME);
			$c->add(PoliticoPeer::ID, $this->politico_id);
			$this->aPolitico = PoliticoPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aPolitico->addOpinions($this);
			 */
		}
		return $this->aPolitico;
	}

	/**
	 * Declares an association between this object and a Opinion object.
	 *
	 * @param      Opinion $v
	 * @return     Opinion The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setOpinionRelatedByOpinionId(Opinion $v = null)
	{
		if ($v === null) {
			$this->setOpinionId(NULL);
		} else {
			$this->setOpinionId($v->getId());
		}

		$this->aOpinionRelatedByOpinionId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Opinion object, it will not be re-added.
		if ($v !== null) {
			$v->addOpinionRelatedByOpinionId($this);
		}

		return $this;
	}


	/**
	 * Get the associated Opinion object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Opinion The associated Opinion object.
	 * @throws     PropelException
	 */
	public function getOpinionRelatedByOpinionId(PropelPDO $con = null)
	{
		if ($this->aOpinionRelatedByOpinionId === null && ($this->opinion_id !== null)) {
			$c = new Criteria(OpinionPeer::DATABASE_NAME);
			$c->add(OpinionPeer::ID, $this->opinion_id);
			$this->aOpinionRelatedByOpinionId = OpinionPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aOpinionRelatedByOpinionId->addOpinionsRelatedByOpinionId($this);
			 */
		}
		return $this->aOpinionRelatedByOpinionId;
	}

	/**
	 * Clears out the collImagens collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addImagens()
	 */
	public function clearImagens()
	{
		$this->collImagens = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collImagens collection (array).
	 *
	 * By default this just sets the collImagens collection to an empty array (like clearcollImagens());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initImagens()
	{
		$this->collImagens = array();
	}

	/**
	 * Gets an array of Imagen objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Opinion has previously been saved, it will retrieve
	 * related Imagens from storage. If this Opinion is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Imagen[]
	 * @throws     PropelException
	 */
	public function getImagens($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(OpinionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collImagens === null) {
			if ($this->isNew()) {
			   $this->collImagens = array();
			} else {

				$criteria->add(ImagenPeer::OPINION_ID, $this->id);

				ImagenPeer::addSelectColumns($criteria);
				$this->collImagens = ImagenPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ImagenPeer::OPINION_ID, $this->id);

				ImagenPeer::addSelectColumns($criteria);
				if (!isset($this->lastImagenCriteria) || !$this->lastImagenCriteria->equals($criteria)) {
					$this->collImagens = ImagenPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastImagenCriteria = $criteria;
		return $this->collImagens;
	}

	/**
	 * Returns the number of related Imagen objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Imagen objects.
	 * @throws     PropelException
	 */
	public function countImagens(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(OpinionPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collImagens === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ImagenPeer::OPINION_ID, $this->id);

				$count = ImagenPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ImagenPeer::OPINION_ID, $this->id);

				if (!isset($this->lastImagenCriteria) || !$this->lastImagenCriteria->equals($criteria)) {
					$count = ImagenPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collImagens);
				}
			} else {
				$count = count($this->collImagens);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Imagen object to this object
	 * through the Imagen foreign key attribute.
	 *
	 * @param      Imagen $l Imagen
	 * @return     void
	 * @throws     PropelException
	 */
	public function addImagen(Imagen $l)
	{
		if ($this->collImagens === null) {
			$this->initImagens();
		}
		if (!in_array($l, $this->collImagens, true)) { // only add it if the **same** object is not already associated
			array_push($this->collImagens, $l);
			$l->setOpinion($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Opinion is new, it will return
	 * an empty collection; or if this Opinion has previously
	 * been saved, it will retrieve related Imagens from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Opinion.
	 */
	public function getImagensJoinPartido($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(OpinionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collImagens === null) {
			if ($this->isNew()) {
				$this->collImagens = array();
			} else {

				$criteria->add(ImagenPeer::OPINION_ID, $this->id);

				$this->collImagens = ImagenPeer::doSelectJoinPartido($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ImagenPeer::OPINION_ID, $this->id);

			if (!isset($this->lastImagenCriteria) || !$this->lastImagenCriteria->equals($criteria)) {
				$this->collImagens = ImagenPeer::doSelectJoinPartido($criteria, $con, $join_behavior);
			}
		}
		$this->lastImagenCriteria = $criteria;

		return $this->collImagens;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Opinion is new, it will return
	 * an empty collection; or if this Opinion has previously
	 * been saved, it will retrieve related Imagens from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Opinion.
	 */
	public function getImagensJoinPolitico($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(OpinionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collImagens === null) {
			if ($this->isNew()) {
				$this->collImagens = array();
			} else {

				$criteria->add(ImagenPeer::OPINION_ID, $this->id);

				$this->collImagens = ImagenPeer::doSelectJoinPolitico($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ImagenPeer::OPINION_ID, $this->id);

			if (!isset($this->lastImagenCriteria) || !$this->lastImagenCriteria->equals($criteria)) {
				$this->collImagens = ImagenPeer::doSelectJoinPolitico($criteria, $con, $join_behavior);
			}
		}
		$this->lastImagenCriteria = $criteria;

		return $this->collImagens;
	}

	/**
	 * Clears out the collOpinionsRelatedByOpinionId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addOpinionsRelatedByOpinionId()
	 */
	public function clearOpinionsRelatedByOpinionId()
	{
		$this->collOpinionsRelatedByOpinionId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collOpinionsRelatedByOpinionId collection (array).
	 *
	 * By default this just sets the collOpinionsRelatedByOpinionId collection to an empty array (like clearcollOpinionsRelatedByOpinionId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initOpinionsRelatedByOpinionId()
	{
		$this->collOpinionsRelatedByOpinionId = array();
	}

	/**
	 * Gets an array of Opinion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Opinion has previously been saved, it will retrieve
	 * related OpinionsRelatedByOpinionId from storage. If this Opinion is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Opinion[]
	 * @throws     PropelException
	 */
	public function getOpinionsRelatedByOpinionId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(OpinionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collOpinionsRelatedByOpinionId === null) {
			if ($this->isNew()) {
			   $this->collOpinionsRelatedByOpinionId = array();
			} else {

				$criteria->add(OpinionPeer::OPINION_ID, $this->id);

				OpinionPeer::addSelectColumns($criteria);
				$this->collOpinionsRelatedByOpinionId = OpinionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(OpinionPeer::OPINION_ID, $this->id);

				OpinionPeer::addSelectColumns($criteria);
				if (!isset($this->lastOpinionRelatedByOpinionIdCriteria) || !$this->lastOpinionRelatedByOpinionIdCriteria->equals($criteria)) {
					$this->collOpinionsRelatedByOpinionId = OpinionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastOpinionRelatedByOpinionIdCriteria = $criteria;
		return $this->collOpinionsRelatedByOpinionId;
	}

	/**
	 * Returns the number of related Opinion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Opinion objects.
	 * @throws     PropelException
	 */
	public function countOpinionsRelatedByOpinionId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(OpinionPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collOpinionsRelatedByOpinionId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(OpinionPeer::OPINION_ID, $this->id);

				$count = OpinionPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(OpinionPeer::OPINION_ID, $this->id);

				if (!isset($this->lastOpinionRelatedByOpinionIdCriteria) || !$this->lastOpinionRelatedByOpinionIdCriteria->equals($criteria)) {
					$count = OpinionPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collOpinionsRelatedByOpinionId);
				}
			} else {
				$count = count($this->collOpinionsRelatedByOpinionId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Opinion object to this object
	 * through the Opinion foreign key attribute.
	 *
	 * @param      Opinion $l Opinion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addOpinionRelatedByOpinionId(Opinion $l)
	{
		if ($this->collOpinionsRelatedByOpinionId === null) {
			$this->initOpinionsRelatedByOpinionId();
		}
		if (!in_array($l, $this->collOpinionsRelatedByOpinionId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collOpinionsRelatedByOpinionId, $l);
			$l->setOpinionRelatedByOpinionId($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Opinion is new, it will return
	 * an empty collection; or if this Opinion has previously
	 * been saved, it will retrieve related OpinionsRelatedByOpinionId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Opinion.
	 */
	public function getOpinionsRelatedByOpinionIdJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(OpinionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collOpinionsRelatedByOpinionId === null) {
			if ($this->isNew()) {
				$this->collOpinionsRelatedByOpinionId = array();
			} else {

				$criteria->add(OpinionPeer::OPINION_ID, $this->id);

				$this->collOpinionsRelatedByOpinionId = OpinionPeer::doSelectJoinUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(OpinionPeer::OPINION_ID, $this->id);

			if (!isset($this->lastOpinionRelatedByOpinionIdCriteria) || !$this->lastOpinionRelatedByOpinionIdCriteria->equals($criteria)) {
				$this->collOpinionsRelatedByOpinionId = OpinionPeer::doSelectJoinUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastOpinionRelatedByOpinionIdCriteria = $criteria;

		return $this->collOpinionsRelatedByOpinionId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Opinion is new, it will return
	 * an empty collection; or if this Opinion has previously
	 * been saved, it will retrieve related OpinionsRelatedByOpinionId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Opinion.
	 */
	public function getOpinionsRelatedByOpinionIdJoinPartido($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(OpinionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collOpinionsRelatedByOpinionId === null) {
			if ($this->isNew()) {
				$this->collOpinionsRelatedByOpinionId = array();
			} else {

				$criteria->add(OpinionPeer::OPINION_ID, $this->id);

				$this->collOpinionsRelatedByOpinionId = OpinionPeer::doSelectJoinPartido($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(OpinionPeer::OPINION_ID, $this->id);

			if (!isset($this->lastOpinionRelatedByOpinionIdCriteria) || !$this->lastOpinionRelatedByOpinionIdCriteria->equals($criteria)) {
				$this->collOpinionsRelatedByOpinionId = OpinionPeer::doSelectJoinPartido($criteria, $con, $join_behavior);
			}
		}
		$this->lastOpinionRelatedByOpinionIdCriteria = $criteria;

		return $this->collOpinionsRelatedByOpinionId;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Opinion is new, it will return
	 * an empty collection; or if this Opinion has previously
	 * been saved, it will retrieve related OpinionsRelatedByOpinionId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Opinion.
	 */
	public function getOpinionsRelatedByOpinionIdJoinPolitico($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(OpinionPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collOpinionsRelatedByOpinionId === null) {
			if ($this->isNew()) {
				$this->collOpinionsRelatedByOpinionId = array();
			} else {

				$criteria->add(OpinionPeer::OPINION_ID, $this->id);

				$this->collOpinionsRelatedByOpinionId = OpinionPeer::doSelectJoinPolitico($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(OpinionPeer::OPINION_ID, $this->id);

			if (!isset($this->lastOpinionRelatedByOpinionIdCriteria) || !$this->lastOpinionRelatedByOpinionIdCriteria->equals($criteria)) {
				$this->collOpinionsRelatedByOpinionId = OpinionPeer::doSelectJoinPolitico($criteria, $con, $join_behavior);
			}
		}
		$this->lastOpinionRelatedByOpinionIdCriteria = $criteria;

		return $this->collOpinionsRelatedByOpinionId;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collImagens) {
				foreach ((array) $this->collImagens as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collOpinionsRelatedByOpinionId) {
				foreach ((array) $this->collOpinionsRelatedByOpinionId as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collImagens = null;
		$this->collOpinionsRelatedByOpinionId = null;
			$this->aUsuario = null;
			$this->aPartido = null;
			$this->aPolitico = null;
			$this->aOpinionRelatedByOpinionId = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseOpinion:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseOpinion::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseOpinion
